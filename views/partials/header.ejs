<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CRUD MVC</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">CRUD MVC</a>
      <div class="d-flex align-items-center">
        <a class="nav-link d-inline text-white" href="/suppliers">Suppliers</a>
        <a class="nav-link d-inline text-white" href="/products">Products</a>


        <% if (typeof userId !== 'undefined' && userId) { %>
          <a href="/users/profile" class="btn btn-sm btn-info ms-3">Trang cá nhân</a>
          <a href="/users/logout" class="btn btn-sm btn-danger ms-3">Đăng xuất</a>


        <% } else { %>
          <a href="/users/login" class="btn btn-sm btn-success ms-3">Đăng nhập</a>
        <% } %>
      </div>
    </div>
  </nav>
  <div class="container">
